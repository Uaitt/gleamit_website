---
export interface TocEntry {
  slug: string;
  text: string;
}

interface Props {
  entries: TocEntry[];
}

const { entries } = Astro.props;
---

<ul class="toc-list">
  {entries.map((entry) => (
    <li><a href={`#${entry.slug}`}>{entry.text}</a></li>
  ))}
</ul>
