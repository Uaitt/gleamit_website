---
import Base from '../layouts/Base.astro';
import Phone from '../components/Phone.astro';
import StoreBadges from '../components/StoreBadges.astro';
import teeth from '../assets/shots/teeth.png';
import home from '../assets/shots/home.png';

const lead =
  'A 2-minute brushing timer, streaks, a 32-tooth map, smile photos, dentist visits and documents. No account, no server, no ads.';

const trust = [
  { icon: 'device', text: 'Everything stays on your device' },
  { icon: 'check', text: 'No account, no Gleamit server' },
  { icon: 'eye-off', text: 'No ads, no analytics' },
  { icon: 'cloud', text: 'Optional backup to your own iCloud or Drive' },
];
---

<Base title="Gleamit - Your mouth, tracked privately" description={lead}>
  <section class="hero">
    <div class="wrap">
      <div>
        <span class="eyebrow">Oral hygiene tracker · Android &amp; iOS</span>
        <h1>Your mouth, over time. On your phone, and nowhere else.</h1>
        <p class="lead">{lead}</p>
        <StoreBadges />
        <p class="fine">Free to use. One optional €9.99 unlock, no subscription.</p>
      </div>
      <div class="hero-art">
        <Phone class="a" src={teeth} alt="The Teeth tab: a 32-tooth map with a filling and a crown marked" loading="eager" />
        <Phone class="b" src={home} alt="The Home tab: today's two brushings, floss and a 12-day streak" loading="eager" />
      </div>
    </div>
  </section>

  <section class="trust">
    <div class="wrap">
      <ul>
        {trust.map(({ icon, text }) => (
          <li>
            <svg viewBox="0 0 24 24" aria-hidden="true">
              {icon === 'device' && <><rect x="5" y="2" width="14" height="20" rx="3" /><path d="M11 18h2" /></>}
              {icon === 'check' && <><circle cx="12" cy="12" r="9" /><path d="M8 12l2.5 2.5L16 9" /></>}
              {icon === 'eye-off' && <path d="M4 4l16 16M9.9 9.9A3 3 0 0 0 14.1 14.1M6.5 6.5C4 8.2 2 12 2 12s4 7 10 7c1.6 0 3-.4 4.3-1M10 5.2C10.6 5.1 11.3 5 12 5c6 0 10 7 10 7s-.8 1.4-2.2 3" />}
              {icon === 'cloud' && <><path d="M3 15a4 4 0 0 0 4 4h11a4 4 0 0 0 .5-8A6 6 0 0 0 7 9a4 4 0 0 0-4 6z" /><path d="M12 12v6M9.5 14.5 12 12l2.5 2.5" /></>}
            </svg>
            {text}
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Anchor targets for the nav until the feature grid (#350) and pricing / FAQ (#351) sections land. -->
  <span id="features" hidden></span>
  <span id="pricing" hidden></span>
  <span id="faq" hidden></span>
</Base>
