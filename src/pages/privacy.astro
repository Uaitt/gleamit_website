---
import { getEntry } from 'astro:content';
import Legal from '../layouts/Legal.astro';

const entry = await getEntry('legal', 'privacy');
if (!entry) throw new Error('Missing src/content/legal/privacy.md');
---

<Legal entry={entry} />
