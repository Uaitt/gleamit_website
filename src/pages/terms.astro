---
import { getEntry } from 'astro:content';
import Legal from '../layouts/Legal.astro';

const entry = await getEntry('legal', 'terms');
if (!entry) throw new Error('Missing src/content/legal/terms.md');
---

<Legal entry={entry} />
